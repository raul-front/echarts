<template>
  <div class="component-flex-page account-employee-list">
    account-employee-list
  </div>
</template>

<script>
import { ref, onMounted, computed, toRaw, reactive, h, toRefs, nextTick } from 'vue'
import { filter } from 'lisa/utils/func'

export default {
  props: {},
  components: {
  },
  setup (props, { emit }) {
    onMounted(() => {
    })

    return {
    }
  },
}
</script>

<style lang="scss">
</style>
